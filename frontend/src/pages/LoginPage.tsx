// File: frontend/src/pages/LoginPage.tsx
// Owner: Agent A (Phase 1) — login form, JWT auth flow
export default function LoginPage() {
  return <div>LoginPage — Agent A fills this in Phase 1</div>;
}
